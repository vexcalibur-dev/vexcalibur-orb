---
description: Run Vexcalibur in a CircleCI Python convenience image.

parameters:
  tag:
    type: string
    default: "3.14"
    description: Tag appended to cimg/python to select the Docker image.

docker:
  - image: cimg/python:<< parameters.tag >>
